{
  "name": "iidx.io",
  "private": true,
  "scripts": {
    "format": "prettier -l '**/*.{js{,x},ts{,x},json}' '**/.babelrc' '!docs/graphql/*' --ignore-path .gitignore",
    "format:fix": "yarn run format --write"
  },
  "devDependencies": {
    "prettier": "^1.14.2"
  }
}
