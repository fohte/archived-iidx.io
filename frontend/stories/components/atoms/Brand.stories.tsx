import { storiesOf } from '@storybook/react'
import * as React from 'react'

import Brand from '@app/components/atoms/Brand'

storiesOf('components|atoms/Brand', module).add('default', () => <Brand />)
